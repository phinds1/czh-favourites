# SP3: DAO Layer — Progress

| Phase | Description                                                              | Status   |
|-------|--------------------------------------------------------------------------|----------|
| 1     | Write delta scripts (`001-create-gis-fav-group.sql`, `002-create-gis-fav-wager.sql`) | complete |
| 2     | Write `FavouriteGroupRecord`, `FavouritesGroupDao`, `FavouritesGroupDaoImpl` | complete |
| 3     | Write `FavouriteWagerRecord`, `FavouritesWagerDao`, `FavouritesWagerDaoImpl` | complete |
| 4     | Write `DaoTestConfig`, `dao-test-schema.sql`, `FavouritesGroupDaoTest`, `FavouritesWagerDaoTest` | complete |
| 5     | Post-SP sign-off: test → coverage → security → refactor → routing map    | complete |

## Notes

### Phase 5 — Post-SP sign-off

- Tests/coverage deferred — norun active (human running tests in background)
- Security review: 0 critical, 0 high, 0 medium, 2 low
  - LOW: `wagerJson` size not checked in DAO — SP5 validator responsibility
  - LOW: `countByPlayer` null-guard — defensive, no action needed
- Refactor pass: clean — all SQL as named constants, no string concatenation, no legacy imports
- Routing map updated: `.requirements/design/routing.md`

### Key design decisions recorded

- DAO stores `WAGER_JSON` as raw String; service layer (SP5) owns ObjectMapper round-trips
- Test schema uses HSQLDB `GENERATED BY DEFAULT AS IDENTITY` (project already uses HSQLDB, not H2)
- Schema DDL in `dao-test-schema.sql` (classpath); prod delta scripts keep DB2 syntax

## SP3 status: DONE — proceed to SP4

## Resume instructions

1. Read `.planning/plans/SP3_DAO_LAYER_PLAN.md`
2. Find the first phase marked **pending** above
3. Read only the **Files to read** listed for that phase
4. Implement, run `./mvn.sh -pl czh-favorites-app compile` (or `test` for Phase 4+), confirm green
5. Update the phase status to **complete** in this file before moving to the next phase
